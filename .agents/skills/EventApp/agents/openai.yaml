interface:
  display_name: "EventApp"
  short_description: "Repo-specific patterns and workflows for EventApp"
  default_prompt: "Use the EventApp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true