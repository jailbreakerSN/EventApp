import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — app package loads', () {
    // Minimal smoke test so `flutter test` has something to run.
    // Replace with real widget / unit tests as features are implemented.
    expect(1 + 1, 2);
  });
}
