{
  "name": "@teranga/firebase-rules-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Firestore and Storage security rules test suite",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase-admin": "^13.0.0",
    "vitest": "^3.2.1"
  }
}
